export type ArtistRatingData = {
  artist_name: string;
  image_url: string;
};

export type RatingData = {
  artist_name: string;
  festival_name: string;
  rating: string;
  year: string;
  comment: string;
};
